<UserControl x:Class="ManufacturingERP.Views.Finance.CostAnalysisView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             mc:Ignorable="d" 
             d:DesignHeight="2000" d:DesignWidth="1100">
    <Grid>
        <Grid.Resources>
            <Style x:Key="CenterTextBlockStyle" TargetType="TextBlock">
                <Setter Property="HorizontalAlignment" Value="Center"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
                <Setter Property="TextAlignment" Value="Center"/>
            </Style>
        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <StackPanel>
                <TextBlock Text="Phân tích giá thành &amp; lợi nhuận" FontSize="18" FontWeight="Bold" Foreground="#1e293b"/>
                <TextBlock Text="Chi tiết chi phí và biên lợi nhuận theo sản phẩm" FontSize="13" Foreground="#64748b"/>
            </StackPanel>
        </Grid>

        <!-- Costing KPIs -->
        <Grid Grid.Row="1" Margin="0,0,0,32">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource CardStyle}" Margin="0,0,16,0" BorderBrush="#D1FAE5" BorderThickness="1">
                <StackPanel Orientation="Horizontal">
                    <Border Background="#F0FDF4" Width="40" Height="40" CornerRadius="20" Margin="0,0,12,0">
                        <Path Data="{StaticResource IconTrendingUp}" Fill="#16A34A" Width="18" Height="18" Stretch="Uniform"/>
                    </Border>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="Tổng doanh thu" FontSize="11" Foreground="#64748b"/>
                        <TextBlock Text="{Binding TotalRevenueText}" FontSize="20" FontWeight="Bold" Foreground="#16A34A"/>
                        <TextBlock Text="4 sản phẩm" FontSize="10" Foreground="#94a3b8"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <Border Grid.Column="1" Style="{StaticResource CardStyle}" Margin="0,0,16,0" BorderBrush="#FEE2E2" BorderThickness="1">
                <StackPanel Orientation="Horizontal">
                    <Border Background="#FEF2F2" Width="40" Height="40" CornerRadius="20" Margin="0,0,12,0">
                        <Path Data="{StaticResource IconTrendingDown}" Fill="#DC2626" Width="18" Height="18" Stretch="Uniform"/>
                    </Border>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="Tổng chi phí" FontSize="11" Foreground="#64748b"/>
                        <TextBlock Text="{Binding TotalCostText}" FontSize="20" FontWeight="Bold" Foreground="#DC2626"/>
                        <TextBlock Text="Giá thành thực tế" FontSize="10" Foreground="#94a3b8"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Style="{StaticResource CardStyle}" Margin="0,0,16,0" BorderBrush="#DBEAFE" BorderThickness="1">
                <StackPanel Orientation="Horizontal">
                    <Border Background="#EFF6FF" Width="40" Height="40" CornerRadius="20" Margin="0,0,12,0">
                        <Path Data="{StaticResource IconDollar}" Fill="#2563EB" Width="18" Height="18" Stretch="Uniform"/>
                    </Border>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="Lợi nhuận" FontSize="11" Foreground="#64748b"/>
                        <TextBlock Text="{Binding ProfitText}" FontSize="20" FontWeight="Bold" Foreground="#2563EB"/>
                        <TextBlock Text="Doanh thu - Chi phí" FontSize="10" Foreground="#94a3b8"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <Border Grid.Column="3" Style="{StaticResource CardStyle}" BorderBrush="#F3E8FF" BorderThickness="1">
                <StackPanel Orientation="Horizontal">
                    <Border Background="#F5F3FF" Width="40" Height="40" CornerRadius="20" Margin="0,0,12,0">
                        <Path Data="{StaticResource IconBox}" Fill="#8B5CF6" Width="18" Height="18" Stretch="Uniform"/>
                    </Border>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="Biên lợi nhuận TB" FontSize="11" Foreground="#64748b"/>
                        <TextBlock Text="{Binding AvgMarginText}" FontSize="20" FontWeight="Bold" Foreground="#8B5CF6"/>
                        <TextBlock Text="Trung bình các SP" FontSize="10" Foreground="#94a3b8"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Overhead Allocation Details -->
        <Border Grid.Row="2" Background="#F8FAFC" CornerRadius="12" Padding="20,16" Margin="0,0,0,32" BorderBrush="#E2E8F0" BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="Tổng chi phí chung (Tháng)" FontSize="12" Foreground="#64748b" Margin="0,0,0,4"/>
                    <TextBlock Text="{Binding TotalMonthlyOverhead}" FontSize="18" FontWeight="Bold" Foreground="#1e293b"/>
                </StackPanel>

                <Border Grid.Column="1" Width="1" Background="#E2E8F0" Margin="24,4"/>

                <StackPanel Grid.Column="2">
                    <TextBlock Text="Tổng giờ sản xuất (Tháng)" FontSize="12" Foreground="#64748b" Margin="0,0,0,4"/>
                    <TextBlock Text="{Binding TotalMonthlyLaborHours}" FontSize="18" FontWeight="Bold" Foreground="#1e293b"/>
                </StackPanel>

                <Border Grid.Column="3" Width="1" Background="#E2E8F0" Margin="24,4"/>

                <StackPanel Grid.Column="4">
                    <TextBlock Text="Hệ số phân bổ (Overhead Rate)" FontSize="12" Foreground="#64748b" Margin="0,0,0,4"/>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding OverheadRatePerHourText}" FontSize="18" FontWeight="Bold" Foreground="#2563EB"/>
                        <TextBlock Text=" / giờ" FontSize="14" Foreground="#94a3b8" VerticalAlignment="Bottom" Margin="4,0,0,2"/>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Profit Visualization -->
        <Grid Grid.Row="3" Margin="0,0,0,32">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <Grid Grid.Row="0" Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Phân tích trực quan" FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center"/>
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <TextBlock Text="Kỳ phân tích biểu đồ:" VerticalAlignment="Center" Margin="0,0,12,0" Foreground="#64748b" FontSize="12"/>
                    <DatePicker SelectedDate="{Binding ChartStartDate}" Width="130" Height="32" Margin="0,0,8,0" Style="{StaticResource ModernDatePickerStyle}"/>
                    <DatePicker SelectedDate="{Binding ChartEndDate}" Width="130" Height="32" Style="{StaticResource ModernDatePickerStyle}"/>
                </StackPanel>
            </Grid>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

            <Border Style="{StaticResource CardStyle}" Margin="0,0,16,0" Height="380">
                <StackPanel>
                    <Grid Margin="0,0,0,16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Lợi nhuận theo nhóm" FontSize="15" FontWeight="SemiBold" VerticalAlignment="Center"/>
                        <ComboBox Grid.Column="1" ItemsSource="{Binding ProductGroups}" SelectedItem="{Binding SelectedProductGroup}" Width="150" Height="30"/>
                    </Grid>
                    <lvc:PieChart Series="{Binding ProfitByGroupSeries}" LegendLocation="Bottom" Height="250" InnerRadius="50"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="1" Style="{StaticResource CardStyle}" Height="380">
                <StackPanel>
                    <TextBlock Text="Khách hàng chiến lược (Lợi nhuận)" FontSize="15" FontWeight="SemiBold" Margin="0,0,0,16"/>
                    <lvc:CartesianChart Series="{Binding ProfitByCustomerSeries}" LegendLocation="None" Height="250">
                        <lvc:CartesianChart.AxisX>
                            <lvc:Axis Title="Số tiền" LabelFormatter="{Binding YAxisFormatter}"/>
                        </lvc:CartesianChart.AxisX>
                        <lvc:CartesianChart.AxisY>
                            <lvc:Axis Labels="{Binding CustomerLabels}"/>
                        </lvc:CartesianChart.AxisY>
                    </lvc:CartesianChart>
                </StackPanel>
            </Border>
            </Grid>
        </Grid>

        <StackPanel Grid.Row="4" Margin="0,0,0,32">
            <Grid Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Chi tiết giá thành sản phẩm" FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center"/>
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <TextBlock Text="Lọc thời gian:" VerticalAlignment="Center" Margin="0,0,12,0" Foreground="#64748b" FontSize="12"/>
                    <DatePicker SelectedDate="{Binding CostStartDate}" Width="130" Height="32" Margin="0,0,8,0" Style="{StaticResource ModernDatePickerStyle}"/>
                    <DatePicker SelectedDate="{Binding CostEndDate}" Width="130" Height="32" Style="{StaticResource ModernDatePickerStyle}"/>
                </StackPanel>
            </Grid>
            <Border Style="{StaticResource CardStyle}" Padding="0">
                <DataGrid ItemsSource="{Binding ProductCosts}" IsReadOnly="True" Style="{StaticResource ModernDataGridStyle}" VerticalScrollBarVisibility="Disabled">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Mã SP" Binding="{Binding Id}" Width="80"/>
                        <DataGridTextColumn Header="Tên sản phẩm" Binding="{Binding Name}" Width="1.5*"/>
                        <DataGridTemplateColumn Header="Lệnh SX" Width="110">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding OrderRef}" Foreground="#2563EB" FontWeight="Medium" VerticalAlignment="Center"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="SL" Binding="{Binding Quantity}" Width="60" ElementStyle="{StaticResource CenterTextBlockStyle}"/>
                        <DataGridTextColumn Header="CP Nguyên liệu" Binding="{Binding MaterialCost, StringFormat={}{0:N0} đ}" Width="*"/>
                        <DataGridTextColumn Header="CP Nhân công" Binding="{Binding LaborCost, StringFormat={}{0:N0} đ}" Width="*"/>
                        <DataGridTextColumn Header="CP Phụ" Binding="{Binding OverheadCost, StringFormat={}{0:N0} đ}" Width="*"/>
                        <DataGridTextColumn Header="Tổng giá thành" Binding="{Binding TotalCost, StringFormat={}{0:N0} đ}" Width="120" FontWeight="Bold"/>
                        <DataGridTextColumn Header="Đơn giá" Binding="{Binding UnitPrice, StringFormat={}{0:N0} đ}" Width="110" Foreground="#64748b"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
            <!-- Pagination Cost -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
                <TextBlock VerticalAlignment="Center" Margin="0,0,16,0" Foreground="#64748b">
                    <Run Text="Trang"/>
                    <Run Text="{Binding CurrentPageCost}" FontWeight="SemiBold" Foreground="#1e293b"/>
                    <Run Text="/"/>
                    <Run Text="{Binding TotalPagesCost}"/>
                </TextBlock>
                <StackPanel Orientation="Horizontal">
                    <Button Style="{StaticResource GhostButtonStyle}" Command="{Binding PreviousPageCostCommand}" IsEnabled="{Binding CanPrevCost}">
                        <Path Data="{StaticResource IconChevronLeft}" Fill="#64748b" Width="14" Height="14" Stretch="Uniform"/>
                    </Button>
                    <Button Style="{StaticResource GhostButtonStyle}" Command="{Binding NextPageCostCommand}" IsEnabled="{Binding CanNextCost}">
                        <Path Data="{StaticResource IconChevronRight}" Fill="#64748b" Width="14" Height="14" Stretch="Uniform"/>
                    </Button>
                </StackPanel>
            </StackPanel>
        </StackPanel>

        <StackPanel Grid.Row="5" Margin="0,0,0,32">
            <Grid Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Phân tích lợi nhuận" FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center"/>
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <TextBlock Text="Lọc thời gian:" VerticalAlignment="Center" Margin="0,0,12,0" Foreground="#64748b" FontSize="12"/>
                    <DatePicker SelectedDate="{Binding ProfitStartDate}" Width="130" Height="32" Margin="0,0,8,0" Style="{StaticResource ModernDatePickerStyle}"/>
                    <DatePicker SelectedDate="{Binding ProfitEndDate}" Width="130" Height="32" Style="{StaticResource ModernDatePickerStyle}"/>
                </StackPanel>
            </Grid>
            <Border Style="{StaticResource CardStyle}" Padding="0">
                <DataGrid ItemsSource="{Binding ProfitAnalysis}" IsReadOnly="True" Style="{StaticResource ModernDataGridStyle}" VerticalScrollBarVisibility="Disabled">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Sản phẩm" Binding="{Binding Name}" Width="2*"/>
                        <DataGridTextColumn Header="Giá thành" Binding="{Binding Cost, StringFormat={}{0:N0} đ}" Width="*"/>
                        <DataGridTextColumn Header="Giá bán" Binding="{Binding Price, StringFormat={}{0:N0} đ}" Width="*"/>
                        <DataGridTextColumn Header="Doanh thu" Binding="{Binding Revenue, StringFormat={}{0:N0} đ}" Width="*"/>
                        <DataGridTemplateColumn Header="Lợi nhuận" Width="*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Profit, StringFormat={}{0:N0} đ}" FontWeight="SemiBold" VerticalAlignment="Center">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Foreground" Value="#16A34A"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Category}" Value="Lỗ">
                                                        <Setter Property="Foreground" Value="#DC2626"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="Biên lợi nhuận" Width="180">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,4">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>
                                        <TextBlock Text="{Binding Margin, StringFormat={}{0:F1}%}" FontSize="12" FontWeight="Medium" Margin="0,0,0,4"/>
                                        <ProgressBar Grid.Row="1" Value="{Binding Margin}" Maximum="100" Height="6" BorderThickness="0" Background="#F1F5F9">
                                            <ProgressBar.Style>
                                                <Style TargetType="ProgressBar">
                                                    <Setter Property="Foreground" Value="#3B82F6"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Category}" Value="Cao">
                                                            <Setter Property="Foreground" Value="#10B981"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Category}" Value="Lỗ">
                                                            <Setter Property="Foreground" Value="#EF4444"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </ProgressBar.Style>
                                        </ProgressBar>
                                    </Grid>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="Phân loại" Width="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border x:Name="badge" Style="{StaticResource StatusBadgeStyle}">
                                        <TextBlock x:Name="txt" Text="{Binding Category}"/>
                                    </Border>
                                    <DataTemplate.Triggers>
                                        <DataTrigger Binding="{Binding Category}" Value="Cao">
                                            <Setter TargetName="badge" Property="Background" Value="#F0FDF4"/>
                                            <Setter TargetName="badge" Property="BorderBrush" Value="#DCFCE7"/>
                                            <Setter TargetName="txt" Property="Foreground" Value="#16A34A"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Category}" Value="Trung bình">
                                            <Setter TargetName="badge" Property="Background" Value="#FFF7ED"/>
                                            <Setter TargetName="badge" Property="BorderBrush" Value="#FFEDD5"/>
                                            <Setter TargetName="txt" Property="Foreground" Value="#EA580C"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Category}" Value="Lỗ">
                                            <Setter TargetName="badge" Property="Background" Value="#FEF2F2"/>
                                            <Setter TargetName="badge" Property="BorderBrush" Value="#FECACA"/>
                                            <Setter TargetName="txt" Property="Foreground" Value="#DC2626"/>
                                        </DataTrigger>
                                    </DataTemplate.Triggers>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
            <!-- Pagination Profit -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
                <TextBlock VerticalAlignment="Center" Margin="0,0,16,0" Foreground="#64748b">
                    <Run Text="Trang"/>
                    <Run Text="{Binding CurrentPageProfit}" FontWeight="SemiBold" Foreground="#1e293b"/>
                    <Run Text="/"/>
                    <Run Text="{Binding TotalPagesProfit}"/>
                </TextBlock>
                <StackPanel Orientation="Horizontal">
                    <Button Style="{StaticResource GhostButtonStyle}" Command="{Binding PreviousPageProfitCommand}" IsEnabled="{Binding CanPrevProfit}">
                        <Path Data="{StaticResource IconChevronLeft}" Fill="#64748b" Width="14" Height="14" Stretch="Uniform"/>
                    </Button>
                    <Button Style="{StaticResource GhostButtonStyle}" Command="{Binding NextPageProfitCommand}" IsEnabled="{Binding CanNextProfit}">
                        <Path Data="{StaticResource IconChevronRight}" Fill="#64748b" Width="14" Height="14" Stretch="Uniform"/>
                    </Button>
                </StackPanel>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
