<Window x:Class="ManufacturingERP.Views.Dialogs.ScheduleImportExportDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Nhập/Xuất lịch làm việc" Height="550" Width="450"
        WindowStartupLocation="CenterScreen" WindowStyle="None" AllowsTransparency="True" Background="Transparent">
    
    <Border Background="White" CornerRadius="16" BorderBrush="#E2E8F0" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" Background="#F8FAFC" CornerRadius="16,16,0,0" Padding="20,15">
                <Grid>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xE10C;" FontFamily="Segoe MDL2 Assets" VerticalAlignment="Center" Margin="0,0,12,0" FontSize="18" Foreground="#2563EB"/>
                        <TextBlock Text="{Binding PageTitle}" FontSize="16" FontWeight="Bold" Foreground="#1E293B" VerticalAlignment="Center"/>
                    </StackPanel>
                    <Button HorizontalAlignment="Right" Click="Close_Click" Background="Transparent" BorderThickness="0" Cursor="Hand">
                        <TextBlock Text="&#xE106;" FontFamily="Segoe MDL2 Assets" Foreground="#64748B" FontSize="14"/>
                    </Button>
                </Grid>
            </Border>

            <!-- Content -->
            <TabControl Grid.Row="1" Background="Transparent" BorderThickness="0" Margin="20,15">
                <TabControl.Resources>
                    <Style TargetType="TabItem">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="TabItem">
                                    <Border Name="Border" Padding="15,8" Margin="0,0,5,15" CornerRadius="8" Background="Transparent">
                                        <ContentPresenter ContentSource="Header" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="Border" Property="Background" Value="#2563EB"/>
                                            <Setter Property="Foreground" Value="White"/>
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="False">
                                            <Setter Property="Foreground" Value="#64748B"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </TabControl.Resources>

                <!-- EXPORT TAB -->
                <TabItem Header="Xuất lịch Excel" IsSelected="{Binding IsExportMode}">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <StackPanel>
                            <TextBlock Text="Chọn thời gian xuất" FontWeight="SemiBold" Margin="0,0,0,10" Foreground="#475569"/>
                            <Grid Margin="0,0,0,20">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0" Margin="0,0,5,0">
                                    <TextBlock Text="Tháng" FontSize="12" Foreground="#94A3B8" Margin="0,0,0,5"/>
                                    <ComboBox Height="35" ItemsSource="{Binding AvailableMonths}" SelectedItem="{Binding Month}"/>
                                </StackPanel>
                                <StackPanel Grid.Column="1" Margin="5,0,0,0">
                                    <TextBlock Text="Năm" FontSize="12" Foreground="#94A3B8" Margin="0,0,0,5"/>
                                    <ComboBox Height="35" ItemsSource="{Binding AvailableYears}" SelectedItem="{Binding Year}"/>
                                </StackPanel>
                            </Grid>

                            <TextBlock Text="Cấu trúc file xuất" FontWeight="SemiBold" Margin="0,0,0,10" Foreground="#475569"/>
                            <ItemsControl ItemsSource="{Binding ExportColumns}" Margin="0,0,0,20">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <UniformGrid Columns="2"/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <CheckBox Content="{Binding Name}" IsChecked="{Binding IsSelected}" Margin="0,5"/>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>

                            <Border Background="#F0F9FF" CornerRadius="8" Padding="12">
                                <TextBlock Text="Mẹo: Bạn có thể xuất lịch để làm mẫu nhập dữ liệu cho tháng tiếp theo." FontSize="11" Foreground="#0369A1" TextWrapping="Wrap"/>
                            </Border>
                        </StackPanel>
                    </ScrollViewer>
                </TabItem>

                <!-- IMPORT TAB -->
                <TabItem Header="Nhập lịch Excel" IsSelected="{Binding IsImportMode}">
                    <StackPanel>
                        <TextBlock Text="Chọn tệp tin Excel" FontWeight="SemiBold" Margin="0,0,0,10" Foreground="#475569"/>
                        <Grid Margin="0,0,0,25">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Border BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="8" Padding="10,0">
                                <TextBlock Text="{Binding SelectedFilePath}" VerticalAlignment="Center" Foreground="#64748B" FontSize="12" TextWrapping="NoWrap"/>
                            </Border>
                            <Button Grid.Column="1" Content="Chọn file" Command="{Binding SelectFileCommand}" Style="{StaticResource SecondaryButtonStyle}" Margin="10,0,0,0" Padding="15,8"/>
                        </Grid>

                        <TextBlock Text="Tùy chọn" FontWeight="SemiBold" Margin="0,0,0,10" Foreground="#475569"/>
                        <CheckBox Content="Cập nhật nếu đã có lịch (Ghi đè)" IsChecked="{Binding IsUpdateMode}" Margin="0,0,0,20"/>

                        <Border Background="#FFF7ED" CornerRadius="12" Padding="15">
                            <StackPanel>
                                <TextBlock Text="Hướng dẫn:" FontWeight="Bold" Foreground="#9A3412" Margin="0,0,0,5"/>
                                <TextBlock Text="1. Các cột bắt buộc: Mã nhân viên, Ngày trực (dd/MM/yyyy), Ca làm việc." FontSize="12" Foreground="#9A3412" Margin="0,0,0,3"/>
                                <TextBlock Text="2. Tên ca làm việc phải khớp với danh sách trong hệ thống." FontSize="12" Foreground="#9A3412"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </TabItem>
            </TabControl>

            <!-- Footer -->
            <Border Grid.Row="2" Background="#F8FAFC" CornerRadius="0,0,16,16" Padding="20,15">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Đóng" Click="Close_Click" Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,12,0" Width="100"/>
                    <Button Content="Bắt đầu" Command="{Binding ProcessCommand}" Background="#2563EB" Foreground="White" Width="120" Height="40">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="8"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>
