<Window x:Class="ManufacturingERP.Views.Dialogs.ConfirmDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Xác nhận" Height="220" Width="400"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False">
    
    <Border CornerRadius="12" Background="White" BorderBrush="#E5E7EB" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="2" Opacity="0.15" Color="Black"/>
        </Border.Effect>
        
        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header & Icon -->
            <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,16">
                <Border Width="40" Height="40" Background="#FEF2F2" CornerRadius="20" Margin="0,0,16,0">
                    <Path Data="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm1 15h-2v-2h2v2zm0-4h-2V7h2v6z" 
                          Fill="#EF4444" Width="20" Height="20" Stretch="Uniform"/>
                </Border>
                <TextBlock x:Name="TitleText" Text="Xác nhận" FontSize="18" FontWeight="Bold" Foreground="#111827" VerticalAlignment="Center"/>
            </StackPanel>

            <!-- Message Content -->
            <TextBlock x:Name="MessageText" Grid.Row="1" TextWrapping="Wrap" FontSize="14" Foreground="#4B5563" LineHeight="20"/>

            <!-- Actions -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
                <Button Content="Hủy bỏ" Click="Cancel_Click" Width="100" Margin="0,0,12,0" Style="{StaticResource GhostButtonStyle}"/>
                <Button Content="Xác nhận xóa" Click="Confirm_Click" Width="120" Background="#EF4444" Foreground="White">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
                            <Setter Property="Background" Value="#EF4444"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#DC2626"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</Window>
