<UserControl x:Class="ManufacturingERP.Views.MasterDataImportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:core="clr-namespace:ManufacturingERP.Core"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1200"
             Background="#F8FAFC"
             Language="vi-VN">
    
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,30">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Left: Back Button -->
            <Button Grid.Column="0" Command="{Binding BackCommand}" Background="Transparent" BorderThickness="0" Cursor="Hand" VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="&#xE112;" FontFamily="Segoe MDL2 Assets" VerticalAlignment="Center" Foreground="#64748B" Margin="0,0,8,0"/>
                    <TextBlock Text="Quay lại danh sách" Foreground="#64748B" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>

            <!-- Center: Title -->
            <TextBlock Grid.Column="1" Text="Nhập danh mục vật tư từ Excel" FontSize="28" FontWeight="Bold" Foreground="#0F172A" 
                       HorizontalAlignment="Center" VerticalAlignment="Center"/>

            <!-- Right: Action Button -->
            <Button Grid.Column="2" Command="{Binding ProcessCommand}" 
                    IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBooleanConverter}}"
                    Background="#2563EB" Foreground="White" Padding="25,12" FontSize="14" FontWeight="SemiBold">
                <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="10"/></Style></Button.Resources>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="&#xE8B5;" FontFamily="Segoe MDL2 Assets" Margin="0,0,10,0" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding IsBusy, Converter={StaticResource BooleanToStringConverter}, ConverterParameter='Đang xử lý...|Bắt đầu nạp dữ liệu'}" 
                               VerticalAlignment="Center"/>
                </StackPanel>
            </Button>
        </Grid>

        <!-- Loading Indicator -->
        <ProgressBar Grid.Row="1" VerticalAlignment="Top" Height="4" IsIndeterminate="True" 
                     Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}"
                     Foreground="#2563EB" Background="Transparent" BorderThickness="0" Panel.ZIndex="10"/>

        <!-- Main Content -->
        <Grid Grid.Row="1" IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBooleanConverter}}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="350"/>
            </Grid.ColumnDefinitions>

            <!-- Left Panel -->
            <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto" Padding="0,0,30,0">
                <StackPanel>
                    <!-- File Selection -->
                    <Border Background="White" CornerRadius="16" Padding="25" Margin="0,0,0,25">
                        <Border.Effect><DropShadowEffect BlurRadius="15" Opacity="0.05" ShadowDepth="0"/></Border.Effect>
                        <StackPanel>
                            <TextBlock Text="CHỌN TỆP DỮ LIỆU NGUỒN" FontSize="12" FontWeight="Bold" Foreground="#64748B" Margin="0,0,0,20"/>
                            
                            <Grid Margin="0,0,0,25">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox Text="{Binding SelectedFilePath}" IsReadOnly="True" Height="45" VerticalContentAlignment="Center" Padding="15,0" Background="#F1F5F9" BorderThickness="0">
                                    <TextBox.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="10"/></Style></TextBox.Resources>
                                </TextBox>
                                <Button Grid.Column="1" Command="{Binding SelectFileCommand}" Content="Chọn file..." Margin="12,0,0,0" Padding="20,0" Background="White" BorderBrush="#E2E8F0" Foreground="#1E293B" FontWeight="Medium">
                                    <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="10"/></Style></Button.Resources>
                                </Button>
                            </Grid>

                            <Border Background="#F8FAFC" CornerRadius="12" Padding="20">
                                <StackPanel>
                                    <CheckBox IsChecked="{Binding IsUpdateMode}" Margin="0,0,0,0">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="Chế độ cập nhật (Update Mode)" FontWeight="Bold" Foreground="#0F172A"/>
                                            <TextBlock Text=" - Nếu mã vật tư đã tồn tại, hệ thống sẽ ghi đè thông tin mới." FontSize="12" Foreground="#64748B" Margin="5,0,0,0"/>
                                        </StackPanel>
                                    </CheckBox>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </Border>

                    <!-- Template Download -->
                    <Border Background="White" CornerRadius="16" Padding="25">
                        <Border.Effect><DropShadowEffect BlurRadius="15" Opacity="0.05" ShadowDepth="0"/></Border.Effect>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Border Background="#ECFDF5" Width="50" Height="50" CornerRadius="12">
                                <TextBlock Text="&#xE118;" FontFamily="Segoe MDL2 Assets" Foreground="#10B981" FontSize="20" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                            </Border>
                            <StackPanel Grid.Column="1" Margin="20,0">
                                <TextBlock Text="Tải file Excel mẫu" FontWeight="Bold" FontSize="16" Foreground="#1E293B"/>
                                <TextBlock Text="Sử dụng file mẫu để đảm bảo dữ liệu được nhập chính xác nhất." Foreground="#64748B" Margin="0,4,0,0"/>
                            </StackPanel>
                            <Button Grid.Column="2" Content="Tải xuống mẫu" 
                                    Command="{Binding DownloadTemplateCommand}"
                                    Background="Transparent" Foreground="#2563EB" FontWeight="Bold" BorderThickness="0" Cursor="Hand"/>
                        </Grid>
                    </Border>
                </StackPanel>
            </ScrollViewer>

            <!-- Right Panel: Instructions -->
            <StackPanel Grid.Column="1">
                <Border Background="White" CornerRadius="16" Padding="25">
                    <Border.Effect><DropShadowEffect BlurRadius="15" Opacity="0.05" ShadowDepth="0"/></Border.Effect>
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                            <Border Background="#DBEAFE" Width="30" Height="30" CornerRadius="15" Margin="0,0,12,0">
                                <TextBlock Text="!" Foreground="#2563EB" FontWeight="Bold" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                            </Border>
                            <TextBlock Text="LƯU Ý KHI NHẬP DỮ LIỆU" FontWeight="Bold" Foreground="#1E293B" VerticalAlignment="Center"/>
                        </StackPanel>

                        <ItemsControl ItemsSource="{Binding Instructions}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="#F8FAFC" CornerRadius="12" Padding="15" Margin="0,0,0,15">
                                        <StackPanel>
                                            <TextBlock Text="{Binding Title}" FontWeight="SemiBold" Foreground="#0F172A" Margin="0,0,0,5"/>
                                            <TextBlock Text="{Binding Content}" FontSize="12" Foreground="#64748B" TextWrapping="Wrap"/>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
